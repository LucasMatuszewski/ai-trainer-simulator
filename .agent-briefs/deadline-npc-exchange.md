# Bounded robot-NPC bubble exchange module
User authorized L-2026-09-03-08 and C72. ~38min left. You are not alone in codebase; preserve others. OWN only NEW src/webmcp/npc-exchange.ts and NEW tests/unit/npc-exchange.test.ts. Parent wires main.ts/tools.ts. No other source/docs edits. Implement immediately TDD (red->green->mutation). Do not research providers.
Implement framework-independent createNpcExchange(deps) coordinator for ONE robot-authored line + ONE agent-authored NPC reply. Runtime agent fictional co-authorship explicitly authorized, no game resource effects.
Suggested API start(npcId,line,reply): {ok,reason?}; update(dt); cancel(); snapshot() returns {npcId,status} or null. Deps: isHumanBusy():boolean; isActive():boolean; getNpc(id):{position:{x,z},visible:boolean}|null; getRobot():{position:{x,z},walking:boolean}; moveTo(id):{ok:boolean,reason?}; holdNpc(id:string|null):void; faceEachOther(id):void; sayRobot(line):void; sayNpc(id,line):void. Can refine names but summarize exact API.
start validates nonempty strings max 120 chars (reject, don't silently truncate), npc visible, robot joined, human not busy, not already exchange. Hold NPC BEFORE moveTo so it stops while robot approaches. If move fails release and return error. State walking. update uses accumulated dt; if human becomes busy/robot leaves/NPC disappears cancel and release. Timeout approach 25s stop/release (deps stopRobot optional required if needed). When robot walking false and distance in [0.7,3.5] face each other, say robot once, transition waiting-reply; after3s say NPC once; after another4s release. If walk ends out of range cancel failure. Every active update faces the pair once arrived. Cancellation/reset stops movement and never emits late lines. No timers; tick driven. Calling cancel safely twice. Start while busy should not replace existing. Tests real coordinator sequence, fallback failures, cancel by human, no overlap/no distant speech, exactly once lines. All effects via deps so unit test without WebGL. No main/tool changes.
Report .agent-briefs/deadline-npc-exchange-result.md including exact public interfaces. Use pnpm test tests/unit/npc-exchange.test.ts (NO -- which runs all here).
Do not commit. Do not push.
