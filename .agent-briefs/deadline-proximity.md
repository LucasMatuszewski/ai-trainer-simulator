# Deadline robot proximity patch
Read docs/LUCAS-FEEDBACK-INDEX.md L-2026-09-03-08 and docs/reviews/2026-09-03-webmcp-playtest.md. Authorized bounded fixes; submission in ~45min. You are not alone: do not revert others. OWN ONLY src/engine/agent-companion.ts, new helper src/engine/agent-approach.ts if needed, tests/unit/agent-companion.test.ts and new tests/unit/agent-approach.test.ts. Do not edit main.ts, tools.ts, version.ts, docs, browser or dev server.
Implement tests first and confirm red, then green. Mutation-check new tests by temporarily reverting your production change then restore.
Fix moveTo(person) walking to exact NPC center. Plan to collision-safe point 1.5-2m from NPC using existing approachSpotFor or equivalent available geometry; inspect content/npc-approach.ts. Prefer no broad pathfinding rewrite. On arrival face NPC; if destination person moved materially, bounded retarget avoiding per-frame resets. Ensure no overlap on settling; rooms unaffected.
Human walkToPoint uses 2.5m distance; increase to ~3m and ensure when already too close it backs away to safe spot, not immediately reports arrived. It must NOT move human camera. On timeout stop path and return honest failure. Parent will handle main hook result. For root's bubble tool expose existing walkToPoint or a new bounded awaitMoveTo(target) ONLY if minimal; coordinate summary of public API additions.
Bubble bug: say() currently passes position.clone() so bubble stays at old place while robot walks. Pass stable live vector to bubble layer; do NOT return mutable vector from getPosition. Persona currently truncated to120 via speech clamp; give persona a separate reasonable cap (500) and keep name/speech behavior.
LookAround should include walking/movingTo from existing state. Preserve other behavior. Tests and typecheck, no full browser needed. Summary to .agent-briefs/deadline-proximity-result.md.
Do not commit. Do not push.
