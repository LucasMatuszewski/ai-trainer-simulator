# GLM 5.3 dialogue enrichment — report

Wrote `src/content/dialogues-more.ts` exporting `MORE_DIALOGUES: Record<string, Record<string, DialogueTree>>` (26 trees, 13 NPCs, ~95 authored nodes, same shape/voice as `dialogues.ts`; `pnpm typecheck` and all 225 existing tests pass, and the new file satisfies every invariant `tests/unit/dialogue-tree.test.ts` enforces on `DIALOGUES` plus stricter ones — explicit unique option ids everywhere, `_end` sentinel, `available` predicate on every tree). Each NPC got exactly two trees: a `more` tree (available while its topic flag is unset — the FIRST option leads to the branch that sets the flag via node-entry `set-flag`) and an `after-*` tree (available only once the flag is set — the NPC remembers and offers something different), so decisions change what dialogue options exist on the next visit, and the per-tree `pickedOptions` memory suppresses re-asked lines on revisits. New branches and flags: bartek → `bartek-shared-consulting-secret` (The Bartek Loop secret) → after-secret co-trainer offer (`bartek-recommended-you`); klaudia → `klaudia-rebranded-you` (smart-not-viral series) → after-rebrand paid-course partnership (`klaudia-course-partner`); marek → `marek-showed-the-log` (the 04:13 heartbeat monitor) → after-log code-review trust (`marek-trusted-review`); zosia → `zosia-opened-up` (the 11-minutes-in-the-car confession) → after-opened protected Training block (`zosia-training-block`); pawel → `pawel-read-the-script` (backup to a dead laptop, ties into Janusz's intel) → after-script mentorship (`pawel-apprentice`); kasia → `kasia-revealed-the-role` (haunted Security Awareness Advocate role) → after-role bands leak / referral bounty (`kasia-leaked-bands`, `kasia-referral-open`); tomek → `tomek-reviewed-pr` (the 2,000-file PR is 1,996 lockfiles + a haiku + Burek) → after-review mentorship (`tomek-apprentice`); ania → `ania-saw-the-funnel` (the webinar funnel tour) → after-funnel season two vs kill-the-persona (`ania-season-two`, `ania-killed-persona`); janusz → `janusz-told-the-flood` (2019 chaos-engineering flood) → after-flood the printer plug / good-coffee keys (`janusz-knows-the-plug`, `janusz-leave-printer`, +caffeine); burek → `burek-standup-observed` (the sales-standup "audit") → after-audit the squeaky-toy contract (`burek-person`); grazyna → `grazyna-showed-the-books` ("trainers who survived" spreadsheet tab) → after-books Syntax Error candle collab (`grazyna-candle-partner`); maciek → `maciek-briefed-you` (the one-slide SCALE deck briefing) → after-brief buzzword poll fight (`maciek-training-buzzword`, `maciek-blockchain-wins`); przemek → `przemek-robot-plan` (TrainerBot 3000 confession + never-deny-redirect plan) → after-robot element-one payoff (`przemek-legend-teacher`). No NPCs were skipped. Not wired into `npcs.ts`/`main.ts` per the brief ("we will wire it in later"); existing `dialogues.ts` untouched so all existing option ids stay stable. First uses of the `caffeine` stat in dialogue content (Tomek coffee tribute +10, Janusz good-coffee +15).
