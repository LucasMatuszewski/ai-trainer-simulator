# Deadline prompt and schema patch
Read docs/LUCAS-FEEDBACK-INDEX.md L-2026-09-03-08 and docs/reviews/2026-09-03-webmcp-playtest.md. User authorizes immediate bounded fixes; about 45 minutes remain for submission. You are not alone in this codebase: preserve others' changes. OWN ONLY src/content/webmcp-help.ts, src/ui/webmcp-help-modal.ts, src/webmcp/tools.ts, src/webmcp/bridge.ts, tests/unit/webmcp-help.test.ts, tests/unit/webmcp-tools.test.ts, tests/unit/webmcp-bridge.test.ts. Do NOT edit main.ts, agent-companion.ts, agent-dialogue.ts, version.ts, docs.
Implement with failing tests first, then pass, and mutation check by temporarily undoing your implementation to confirm new focused tests fail, restoring it. Keep logs concise.
1. Strengthen copyable AGENT_PROMPT with office IT trainer lore, dry IT Crowd/Silicon Valley style, consistent robot persona using only relevant user-shared context, LARP improvisation, natural walking and occasional gestures. Native host WebMCP tool discovery first. Raw document.modelContext.getTools/executeTool only IF these functions exist and host permits JS access; do NOT claim ordinary browsers have them. Let human start game. get_instructions first; join once; observe; greet human using start_conversation. Prioritize supply over gestures. Wait 10 seconds per call, rearm on idle. Recover errors via pending context. Robot-only camera/dialogue/time/resources boundaries; stop on user request/feedback. No need to include code sample in ordinary prompt.
2. Update AGENT_INSTRUCTIONS in tools.ts with same lore and priority, truthful limitations (human-control tools ARE exposed but prohibited in coworker role), movement starts asynchronously, no universal no-loss guarantee, no need to change broker protocol. Existing get_pending described as recovery/peek, wait primary. Numeric wait default still 25 in broker unless parent changes, so recommend explicit 10 rather than falsely claiming default10.
3. Correct published options schema to runtime 1-4 string OR {text,ends:boolean} entries for both supply_dialogue and start_conversation, via minimal richer schema support in tools and bridge; preserve compatibility. Include examples with ends:true, appropriate limits if easy. Parent will add a new NPC conversation tool after your work; no need for it here.
4. Fix FAQ that blames all silence on not listening; explain host/model delay and resume/check. Avoid unverified claims about exact model support; say availability depends on host/account and detected tools.
5. Correct UI 'no cost' wording: no game-side API key/backend; user's AI subscription/usage applies. No styling redesign.
Run focused tests and typecheck. Do not run browser or restart server. Write summary .agent-briefs/deadline-prompt-result.md with test/mutation results.
Do not commit. Do not push.
