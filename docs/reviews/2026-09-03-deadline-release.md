# Deadline coworker patch — 2026-09-03

Scope authorized by Lucas in L-2026-09-03-08 through -10; C-72/C-73; ADR 0008 D-44. Lucas owns publication, the submission form and recording. This task prepares local verified fixes.

## Delivered

- Native WebMCP discovery first, conditional modelContext fallback, office lore and embodied roleplay, human-control boundaries, short listening waits and recovery guidance.
- Accurate dialogue option schemas supporting strings and `{text, ends}`; concrete examples.
- Shared NPC/robot hover-label positioning reset, including viewport offsets. Robot speech follows its live position.
- Three-metre human approach; safe NPC approach rings from 1.75 to 2.75 metres; destinations avoid the human observer. This is endpoint clearance, not continuous pedestrian collision avoidance.
- `agent_talk_to_npc` accepts an agent-authored starter and fictional NPC reply, stages the pair, shows sequential bubbles, and cancels when the human interacts. It does not execute NPC quests or open the human's dialogue.
- Human greetings reject failed/stale approaches and respect blocking overlays before and after walking.
- Both copy buttons include the loaded page origin/path, so production opens https://play.devpowers.com and development opens localhost. Query strings and fragments are excluded.

## Verification

- Full unit suite: 68 files, 705 tests passed. Typecheck and production build passed.
- Tests failed before new behavior was added. Targeted mutations caught label positioning, dialogue schema, proximity, NPC exchange timing/cleanup, hardcoded localhost and removed human-busy guards.
- Real-browser coworker test verifies actual spacing, human pose preservation, visible sequential robot/NPC lines and matching console/title build identity. The browser uses a test registration host; the real built-in browser independently discovered the new tool and returned the updated get_instructions through its native WebMCP adapter.
- Independent code review: PASS after fixing stale human-position and blocking-overlay findings. Details in `.agent-briefs/deadline-review-result.md`.
- Screenshots: `screenshots/deadline-build-title.png`, `screenshots/deadline-robot-human.png`, `screenshots/deadline-robot-npc.png`. The NPC composition shows two separate actors, the robot facing Bartek, and a readable NPC response. Existing HUD overlays can hide part of the robot bubble near the edge. Human avatar is absent by design in first person; room, desks, lighting and characters are visible, with no exterior/roof regression.
- The project-requested `agy` screenshot description was attempted but its noninteractive reader denied file access; no permission settings were bypassed. Primary image inspection and independent Codex image review are the available visual evidence.

## Deferred and ownership

Durable dialogue delivery, event IDs/acknowledgement and complete reconnect handling remain deferred. The prompt improves listening practice but does not guarantee delivery across host disconnections. The robot authors both sides of staged NPC exchanges; NPC replies are not generated by a second model. Lucas remains the visual QA and publisher; no deployment, visibility change or push was performed by this task.
